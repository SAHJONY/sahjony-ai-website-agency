{
  "name": "frontdeskagents-website-factory",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Full-stack AI website factory — sell premium websites to local businesses.",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node --test"
  },
  "engines": {
    "node": "22.x"
  }
}
